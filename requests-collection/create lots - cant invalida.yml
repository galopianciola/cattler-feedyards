info:
  name: create lots - cant invalida
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/api/lots/"
  headers:
    - name: Authorization
      value: Token {{AUTH_TOKEN}}
  body:
    type: json
    data: |-
      {
        "quantity": 0,
        "entry_date": "2026-06-01",
        "average_weight": 350.5
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
