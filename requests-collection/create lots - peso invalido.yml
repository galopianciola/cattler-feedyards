info:
  name: create lots - peso invalido
  type: http
  seq: 4

http:
  method: POST
  url: "{{BASE_URL}}/api/lots/"
  headers:
    - name: Authorization
      value: Token {{AUTH_TOKEN}}
  body:
    type: json
    data: |-
      {
        "quantity": 10,
        "entry_date": "2026-06-01",
        "average_weight": 0
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
