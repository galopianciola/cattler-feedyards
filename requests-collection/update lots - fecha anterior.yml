info:
  name: update lots - fecha anterior
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{BASE_URL}}/api/lots/{{LOT_ID}}/"
  headers:
    - name: Authorization
      value: Token {{AUTH_TOKEN}}
  body:
    type: json
    data: |-
      {
        "from_date": "2020-01-01",
        "average_weight": 400
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
