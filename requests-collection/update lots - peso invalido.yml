info:
  name: update lots - peso invalido
  type: http
  seq: 9

http:
  method: PATCH
  url: "{{BASE_URL}}/api/lots/{{LOT_ID}}/"
  headers:
    - name: Authorization
      value: Token {{AUTH_TOKEN}}
  body:
    type: json
    data: |-
      {
        "from_date": "2026-06-05",
        "average_weight": 0
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
